pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }

        stage('Build & Push Docker Image') {
            steps {
                script {
                    withDockerRegistry([credentialsId: 'dockerhub-credentials', url: '']) {
                        // 1. Build the image locally with the build number
                        sh "docker build -t iamvyshnav/nodejs-app:${env.BUILD_ID} ."
                        
                        // 2. Push ONLY this version tag to save memory and network threads
                        sh "docker push iamvyshnav/nodejs-app:${env.BUILD_ID}"
                        
                        // 3. Tag it as latest locally for local testing if needed
                        sh "docker tag iamvyshnav/nodejs-app:${env.BUILD_ID} iamvyshnav/nodejs-app:latest"
                    }
                }
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                script {
                    // Update deployment.yaml with the fresh build ID automatically
                    sh "sed -i 's|image: iamvyshnav/nodejs-app:.*|image: iamvyshnav/nodejs-app:${env.BUILD_ID}|g' deployment.yaml"
                    
                    // Trigger deployment to Minikube
                    sh 'kubectl apply -f deployment.yaml'
                    sh 'kubectl apply -f service.yaml'
                }
            }
        }
    }
}

